import { HighlightBorderWithDateDirective } from './highlight-border-with-date.directive';

describe('HighlightBorderWithDateDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBorderWithDateDirective();
    expect(directive).toBeTruthy();
  });
});
